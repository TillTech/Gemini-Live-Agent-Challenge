{
  "name": "@tilly-live-ops/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@google/genai": "^1.45.0"
  },
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}